# Образец файла для настройки секретов GitHub Actions
# Скопируйте этот файл в .env и заполните реальными значениями
# ВАЖНО: Файл .env НЕ должен попадать в репозиторий (добавлен в .gitignore)

# ============================================
# Секреты для деплоя на сервер (SSH)
# ============================================

# Адрес сервера для деплоя (IP или домен)
SSH_HOST=your-server.example.com

# Имя пользователя для SSH-подключения
SSH_USER=deploy

# Приватный SSH-ключ (содержимое файла ~/.ssh/id_rsa или id_ed25519)
# Скопируйте полностью, включая -----BEGIN и -----END-----
SSH_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
...ваш приватный ключ...
-----END OPENSSH PRIVATE KEY-----"

# ============================================
# Секреты для GitHub Container Registry (опционально)
# ============================================

# Токен для доступа к ghcr.io на сервере (для pull приватных образов)
# Создайте Personal Access Token с правами read:packages
# https://github.com/settings/tokens/new?scopes=read:packages
GHCR_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Как добавить секреты в GitHub:
# ============================================
# 1. Перейдите в репозиторий на GitHub
# 2. Settings → Secrets and variables → Actions
# 3. Нажмите "New repository secret"
# 4. Добавьте каждый секрет:
#    - SSH_HOST
#    - SSH_USER
#    - SSH_PRIVATE_KEY
#    - GHCR_TOKEN (опционально, если образ приватный)

